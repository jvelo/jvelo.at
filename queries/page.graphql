query page($slug: String!) {
    page(uid: $slug, lang: "en-gb") {
        title
        subtitle
        body {
            ... on PageBodyContent {
                type
                primary {
                    text
                }
            }
        }
        _meta {
            id
            uid
        }
    }
}
