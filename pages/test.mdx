import Page from "../components/SiteLayout";
import { Button } from "../components/Button/Button";

export const meta = {
  title: "Markdown/MDX with Next.js",
  description: "An example MDX page that renders within the site layout",
};

# Hello, world!

<Button>Here is a button</Button>

```js
export const Button = ({ children }) => <button>${children}</button>;
```

```js
export default yield 42
```

export default ({ children }) => <Page meta={meta}>{children}</Page>;
